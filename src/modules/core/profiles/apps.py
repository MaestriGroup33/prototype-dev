from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    name = "src.modules.core.profiles"
    verbose_name = (
        "Perfis - Gerenciamento de perfis, quer seja de usuarios, clientes, "
        "alunos ou leads, enfim, todos que passaram ou fazem parte da "
        "Maestri.group"
    )
