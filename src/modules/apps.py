from django.apps import AppConfig


class AppsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "modules"
    verbose_name = "Aplicativos - Aqui é onde fazemos o Maestri.app ser real..."
